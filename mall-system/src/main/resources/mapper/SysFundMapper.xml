<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tireba.mall.system.mapper.SysFundMapper">
    <!--<resultMap id="baseResultMap" type="com.tireba.mall.system.entity.SysFund">
        <id property="id" column="type" jdbcType="INTEGER"></id>
        <result property="type" column="type" jdbcType="INTEGER"></result>
        <result property="money" column="money" jdbcType="VARCHAR"></result>
        <result property="unit" column="unit" jdbcType="VARCHAR"></result>
    </resultMap>-->

    <sql id="Base_Column_List">
        f.id, f.user_id, f.type, f.money, f.unit, f.flow_type, f.earning_type, f.remark, f.create_time, f.update_time
    </sql>
    <insert id="insertUserFlowInfo">
        insert into sys_fund(id, user_id, type, money, unit, flow_type, earning_type, remark, create_time, update_time)
        values(#{id}, #{userId}, #{type}, #{money}, #{unit}, #{flowType}, #{earningType}, #{remark},#{createTime}, #{updateTime})
    </insert>

    <select id="queryFlowListByUserId" resultType="com.tireba.mall.system.entity.vo.SysFundVo">
        select
        u.username,
        <include refid="Base_Column_List"></include>
        from sys_fund f
        left join sys_user u on u.id = f.user_id
        where 1 = 1
        and f.user_id = #{userId}
        <if test="type != null">
            f.type = #{type}
        </if>
        order by f.create_time, f.id desc
    </select>

</mapper>
